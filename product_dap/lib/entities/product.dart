class Product {
  String name;
  String description;
  double price;

  Product({
    required this.name,
    required this.description,
    required this.price,
  });
}
class Usuario {
  String username;
  String password;

  Usuario(this.username, this.password);
}

